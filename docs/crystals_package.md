# Crystals Package

This package contains the crystal-native Grax contract boundary only:

- `api/types.go`
- `server/routes.go`
- `server/grax_consequence_contract.go`

It does not include the broader brullama application tree, model weights, or generated media.
